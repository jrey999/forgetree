[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "forgetree"
version = "0.1.0"
description = "A CLI tool"
requires-python = ">=3.9.19"

[project.scripts]
forgetree = "forgetree.cli.cli:main"